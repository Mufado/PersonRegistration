{
  // I know that these JWT informations are sensible and should NOT
  // be saved here. The appropriate place for it would be in environment
  // variables or a secure vault, but since this isn't a real commercial
  // project that needs security, I'll leave it here for simplicity.

  "Jwt": {
    "Key": "chave-totalmente-segura-para-esse-crud-magnifico",
    "Issuer": "PersonRegistrationApi",
    "Audience": "PersonRegistrationClient"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
